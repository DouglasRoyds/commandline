sudo docker run image:tag command                           Default tag is "latest"
sudo docker run ubuntu:14.04 /bin/echo 'Hello world'        One-off command
sudo docker run -it ubuntu:14.04 /bin/bash                     -i    Keep stdin open
                                                               -t    Attach a tty to stdin
                                                               --rm  Automatically remove the container when it exits
sudo docker run -d ubuntu:14.04 longrunningcommand             -d    Daemonize
sudo docker run -dP image                                   If the Dockerfile specified a default CMD
                                                               -P    Publish all exposed container ports as host interfaces
                                                               -p 8080:5000
                                                                     Publish on this host port: this container port
sudo docker port container 5000                             Which host port is container port 5000 mapped to?

sudo docker ps                                              List running containers
                                                               -l    Just the last one
                                                               -a    All, even stopped ones
sudo docker images                                          List images stored on this host (/var/lib/docker/)
                                                               -a    All, including intermediate build layers

sudo docker logs container                                  By container ID or name
                                                               -f    Follow
sudo docker top container                                   Processes running inside the container
sudo docker inspect container
sudo docker inspect -f '{{ .NetworkSettings.IPAddress }}' container
                                                            Low-level JSON information about the container
                                                               -f    Just this bit (Go template)

sudo docker stop container
sudo docker start container                                 Start it again
sudo docker restart container                               Stop and restart it

sudo docker search string                                   Searches Docker Hub
sudo docker pull ubuntu                                     Pre-fetch it
sudo docker pull username/imagename                         Defaults to "latest"
sudo docker pull username/imagename:tag
sudo docker pull hostname.domain:port/imagename             From a private repository.
                                                            Must have the dot or the : to distinguish the URL from a username
sudo docker push username/imagename                         To Docker Hub at https://registry.hub.docker.com
sudo docker push hostname.domain:port/imagename             To a private repository.

sudo docker commit -m="Message" -a="Authorname" container username/imagename:tag
                                                            Creates a new image from the container's changes.
                                                            The container will be paused (if necessary) during the commit.
sudo docker tag imageid username/imagename:tag              Tag an image after the fact

sudo docker build dirname|Dockerfile                           --no-cache  Allow apt-get update to achieve something
sudo docker build -t="username/imagename:tag" dirname          -t          Repository name and optional tag

sudo docker rm container                                    Delete it (once it's stopped)
                                                               -f    Stop it and delete it
sudo docker rm $(sudo docker ps -aq)                        Delete the lot
sudo docker rmi imagename                                   Delete an image from this host (as opposed to a container)
sudo docker rmi $(sudo docker images | gawk '/^<none>/{print $3}')
                                                            Delete all unnamed images

